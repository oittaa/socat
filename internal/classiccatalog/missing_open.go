package classiccatalog

// Windows binary/text/noinherit descriptor modes were completed by the
// Windows descriptor-mode compatibility work. Keep this family file so future
// O_* platform gaps have one ownership point.
var expectedMissingOpen = map[string]Gap{}
